项目核心：

1. 将连续的K线数据转换为离散的token序列
2. 通过滑动窗口（或其他算法）对K线序列进行单元划分（例如：每3根K线作为一个基本单元）
3. 使用机器学习算法（无监督聚类方法（如K-means））发现有限的市场模式，形成"词汇表"（50-500个tokens）
4. 将实时K线流转换为token序列，使其可以被NLP模型处理

技术要点：

- 不关心每个token的人类可解释含义，这是机器对市场的理解
- 使用现代语言模型（如GPT架构）处理token序列
- 支持预测、生成、异常检测等下游任务# candleGPT
